# This file has been auto-generated by i3-config-wizard(1).
# i3 config file (v4)

#Windows Key 
set $mod Mod4

#Wallpaper
exec --no-startup-id feh --bg-scale '/home/david/dotfiles/images/wallpaper.jpg'
exec --no-startup-id compton &

#i3-Gaps specific
for_window [class="^.*"] border pixel 2 
gaps inner 15
gaps outer 15 

# Audio Keys
bindsym XF86AudioMute exec --no-startup-id amixer -q set Master toggle
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q sset Master 3%+
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q sset Master 3%-

# Font
font pango:Roboto Light 10 

# Colours
set $light		#b48ead
set $dark		#2b303b
set $urgent		#bf616a

client.focused	$light	$light	$dark	$light
client.urgent	$urgent	$urgent	$urgent	#urgent

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+w kill

# start rofi but only show .desktop
bindsym $mod+space exec j4-dmenu-desktop --no-generic --dmenu='rofi -show run -lines 3 -eh 2 -width 100 -padding 460  -bw 0 -bc "$light" -bg "$light" -fg "$dark" -hlbg "$light" -hlfg "#9575cd" -font "Roboto Light 18" -dmenu -i -p "시작: "'

# i3 lock
bindsym $mod+Escape exec --no-startup-id ~/dotfiles/scripts/lock.sh 

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+b split h 

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+a layout toggle split
bindsym $mod+d layout tabbed

# toggle tiling / floating
bindsym $mod+s floating toggle

# Monitor 1
set $workspace1 "1"
set $workspace2 "2"
set $workspace3 "3"
set $workspace4 "4"
set $workspace5 "5"

# Monitor 2
set $workspace6 "6"
set $workspace7 "7"
set $workspace8 "8"
set $workspace9 "9"
set $workspace10 "10"

# Set default workspace out
workspace $workspace1 output HDMI2
workspace $workspace2 output HDMI2
workspace $workspace3 output HDMI2
workspace $workspace4 output HDMI2
workspace $workspace5 output HDMI2

workspace $workspace6 output VGA1
workspace $workspace7 output VGA1
workspace $workspace8 output VGA1
workspace $workspace9 output VGA1
workspace $workspace10 output VGA1

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5

bindsym $mod+Ctrl+1 workspace $workspace6
bindsym $mod+Ctrl+2 workspace $workspace7
bindsym $mod+Ctrl+3 workspace $workspace8
bindsym $mod+Ctrl+4 workspace $workspace9
bindsym $mod+Ctrl+5 workspace $workspace10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Ctrl+Shift+1 move container to workspace $workspace6
bindsym $mod+Ctrl+Shift+2 move container to workspace $workspace7
bindsym $mod+Ctrl+Shift+3 move container to workspace $workspace8
bindsym $mod+Ctrl+Shift+4 move container to workspace $workspace9
bindsym $mod+Ctrl+Shift+5 move container to workspace $workspace10

# Program specific workspaces
assign [class="Wine"] $workspace5
assign [class="Steam"] $workspace5
assign [class="dota2"] $workspace5

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3blocks -c ~/.config/i3/i3blocks
        tray_output primary
	colors {
		background		$dark
		separator		$light
		focused_workspace	$light	$light	$dark
		urgent_workspace	$urgent	$urgent	$dark
		
	}
}
